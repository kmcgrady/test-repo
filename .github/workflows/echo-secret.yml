name: Echo test secret

on:
  workflow_dispatch:  # run manually from the Actions tab

jobs:
  echo-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Show masked secret
        run: |
          echo "Secret is: $MY_TEST_SECRET"
        env:
          MY_TEST_SECRET: ${{ secrets.MY_TEST_SECRET }}
